use crate::{board_repr::board::Board, move_generation::generator::MoveGenerator};

pub struct Search;

impl Search {
    pub fn alpha_beta(alpha: i16, beta: i16, depth: i8, board: &Board, mg: &MoveGenerator) {}
}
